{
  "mcpServers": {
    "inline-dialogue": {
      "command": "uv",
      "args": [
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "run",
        "inline-dialogue-mcp"
      ]
    }
  }
}
